[pytest] #配置pytest的默认行为，要放在主函数同级目录下才能生效，不管用命令行还是main函数执行都会触发读取ini文件
addopts = -vs --alluredir ./report/temp -p no:warnings --clean-alluredir


testpaths = ./testcase
#测试的目录,不要写错


filterwarnings =
    error
    ignore: UserWarning
;markers =
;    smoke: 冒烟测试
;    system: System测试